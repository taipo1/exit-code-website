const SecontionTitle = ({ label }) => (
  <h2 className="text-stroke -translate-y-0 font-grotesk text-[108px] font-semibold text-transparent text-stroke-white">
    {label}
  </h2>
);

export default SecontionTitle;
